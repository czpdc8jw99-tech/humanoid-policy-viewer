# 动作监控结果分析

## 🔴 发现的关键问题

### 1. 动作严重不对称
- **对称性比例**：0.19-0.44（应该 > 0.7）
- **左腿平均幅度**：0.5-1.0
- **右腿平均幅度**：2.3-2.9（是左腿的 2-3 倍！）

### 2. 动作值异常大
- **最大动作值**：9-13（远超正常范围）
- **正常范围**：应该在 [-2, 2] 左右

### 3. 问题持续存在
- 所有帧都显示 `❌ 动作不对称`
- 说明这不是偶发现象，而是系统性问题

---

## 📊 需要查看的详细信息

请展开某一帧（例如 Frame 150）的具体数值，特别是：

### 1. 原始策略输出（Raw action values）
- 左腿的6个关节的原始动作值
- 右腿的6个关节的原始动作值
- 这些值是在应用 `action_scale` 和 `action_clip` 之前的

### 2. 处理后的动作值（Clamped actions）
- 左腿的6个关节的处理后动作值
- 右腿的6个关节的处理后动作值
- 这些值是在应用 `action_scale` 和 `action_clip` 之后的

### 3. 目标关节位置（Scaled targets）
- 左腿的6个关节的目标位置
- 右腿的6个关节的目标位置

---

## 🔍 可能的原因

### 原因1：策略输出本身不对称
- 策略模型输出的原始动作值就不对称
- 可能是策略训练的问题，或者模型本身的问题

### 原因2：动作处理逻辑错误
- `action_scale` 或 `action_clip` 的应用可能有问题
- 左右腿的 `action_scale` 可能不同

### 原因3：动作映射错误
- 动作值到关节的映射可能有误
- 左右腿的映射可能不一致

---

## 💡 下一步行动

1. **查看详细数值**：展开某一帧的具体数值
2. **检查原始输出**：查看策略的原始输出是否对称
3. **检查处理逻辑**：验证 `action_scale` 和 `action_clip` 是否正确应用
4. **检查映射**：验证动作到关节的映射是否正确

---

## 📋 请提供的信息

请展开 Frame 150 或 Frame 180 的详细数值，特别是：

```javascript
// 在控制台运行这个命令，查看 Frame 150 的详细动作值
const pr = window.demo.policyRunner || window.demo.policyRunners?.[0];
const actions = pr.lastActions;
const leftLegIndices = [0, 3, 6, 9, 13, 17];
const rightLegIndices = [1, 4, 7, 10, 14, 18];

console.log('=== 左腿动作值（详细）===');
leftLegIndices.forEach(i => {
  console.log(`  [${i}] ${pr.policyJointNames[i]}:`, {
    action: actions[i].toFixed(4),
    actionScale: pr.actionScale[i].toFixed(4),
    defaultPos: pr.defaultJointPos[i].toFixed(4),
    targetPos: (pr.defaultJointPos[i] + pr.actionScale[i] * actions[i]).toFixed(4)
  });
});

console.log('=== 右腿动作值（详细）===');
rightLegIndices.forEach(i => {
  console.log(`  [${i}] ${pr.policyJointNames[i]}:`, {
    action: actions[i].toFixed(4),
    actionScale: pr.actionScale[i].toFixed(4),
    defaultPos: pr.defaultJointPos[i].toFixed(4),
    targetPos: (pr.defaultJointPos[i] + pr.actionScale[i] * actions[i]).toFixed(4)
  });
});
```
