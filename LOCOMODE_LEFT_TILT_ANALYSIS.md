# LocoMode 左倾与平衡逻辑分析

## 重心为什么“应该”回正？

- 策略输入里有 **重力在机体系的投影**（`ProjectedGravityB`）和 **机身角速度**（`RootAngVelB`）。
- 当机身向左倾时：
  - 重力在 body 的 y 分量会偏离 0（例如 gy > 0 表示向左倾）；
  - 角速度会反映正在向左倒的旋转。
- 策略在训练时学的是：**看到“向左倾”的观测 → 输出“往右修正”的动作**（例如右髋/右肩/右踝 roll 朝把身体拉回正直的方向）。
- 所以从设计上，**重心是会通过策略输出被“拉回正”的**；上半身会往右边平衡，前提是：
  1. 观测（重力、角速度）符号和训练时一致；
  2. 动作按“策略语义”正确施加到对应关节，且左右符号约定一致。

## 为什么会出现“一直往左倒”？

可能原因只有几类：

1. **观测约定不一致**  
   重力或角速度在训练时是某种符号/坐标系，我们在网页里给的相反或不同轴，策略就会“以为在往右倾”而继续往左拉。

2. **动作施加的左右约定不一致**  
   策略输出的“右髋 roll 正”在训练里表示“身体往右拉回”，但在我们仿真里若被当成“右髋往反方向”用，就会变成往左加重。

3. **右侧 roll 关节的“正方向”与训练相反**  
   训练用的 G1 模型里，右侧 roll 的 joint 正方向可能和当前 viewer 的 G1 定义相反（URDF/XML 里左右镜像常见）。这样策略输出的“往右修正”在我们模型里会变成“往左”。

## 已做的修改（v9.0.8）

- **`flip_right_roll_sign`（默认 true）**  
  对 **右侧三个 roll 关节**（right_hip_roll、right_shoulder_roll、right_ankle_roll）的**目标位置**做一次“关于 default 的镜像”：
  - `effectiveTarget[i] = 2 * default[i] - actionTarget[i]`
  - 这样策略输出的“正/负”在我们模型里会被翻成另一侧，用来抵消“训练 vs 我们模型”的左右约定差异。
- **诊断日志**  
  前几帧会打印 L/R 的 hip_roll、shoulder_roll、ankle_roll 的**原始动作**（策略直接输出），便于看：
  - 左倾时策略是否在输出“往右修正”（例如 R 侧与 L 侧符号相反）；
  - 若打开 `flip_right_roll_sign` 后变成往右倾，说明确实是右侧符号反了。

## 如何用控制台日志判断

- 看 **`[LocoMode action 0/1/2]`** 里：
  - **Step 0**：直立、观测对称，动作一般也接近对称。
  - **Step 1/2**：已经左倾时，若策略正常，应看到 **R 侧 roll 与 L 侧符号相反**（例如一边正一边负），表示在往右修正。
- 若 **Step 1 已经左倾，但动作仍然左右同号或很小**：可能是观测（重力/角速度）不对，策略“不知道”自己在左倾。
- 若 **动作明显是往右修正（L/R 反号），但机身仍往左倒**：多半是施加到电机时左右或符号反了；**开启 `flip_right_roll_sign`** 再试。
- 若 **开启 flip 后变成往右倒**：说明右侧 roll 在我们模型里确实和训练时反了，保持 `flip_right_roll_sign: true` 即可。

## 初始高度

- 已改为 **0.8**（与 g1.xml 及前两策略一致），原先 0.793 与 FSMDeploy 训练一致但可能使脚底接触略不同。
- 若仍左倾，可再试 0.793 或微调 0.79–0.81 对比。

## 若仍不平衡

可再检查：

- **第一步观测**：直立时 `ProjectedGravityB` 应为 (0,0,-1)，`RootAngVelB` 接近 0；若第一步就有偏差，策略会误判倾斜。
- **第一步动作**：控制台 `[LocoMode action 0]` 看 Step 0 的 L/R roll 是否接近对称；若 Step 0 就明显不对称，可能是 warm-up 后 `lastActions` 或观测导致。
- 角速度是 **world 系还是 body 系** 是否与 FSMDeploy 一致；
- 重力投影公式是否与 FSMDeploy 完全一致（已按 `get_gravity_orientation` 对齐）；
- 是否还有其他关节（非 roll）存在左右或符号差异。
